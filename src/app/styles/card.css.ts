import React from "react";
import styled from "styled-components";

const css = {
  CardContainer: styled.div`
    display: block;
    position: relative;
    width: 300px;
    height: 300px;
    font-size: 14px;
    text-align: center;
  `,
};

export default css;
